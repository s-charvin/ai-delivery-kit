interface:
  display_name: "UI Requirement Mapping"
  short_description: "Map requirement slices to Figma evidence"
  default_prompt: "Use $ui-requirement-mapping to bind this sub-requirement to screenshot-backed Figma nodes and update the mapping artifacts."
