interface:
  display_name: "UI Truth Mapping"
  short_description: "Map Figma design into a canonical HTML UI contract v2"
  default_prompt: "Use $ui-truth-mapping to bind requirement-scoped Figma evidence to this sub-requirement and freeze one ui-contract.html per unit (with hydrated state preview)."
