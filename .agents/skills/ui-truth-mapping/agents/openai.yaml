interface:
  display_name: "UI Truth Mapping"
  short_description: "Map Figma design into a canonical HTML UI contract v2"
  default_prompt: "Use $ui-truth-mapping to freeze ui-contract.html per unit: static canvas + review-panel 动态 UI 效果 table for motion audit."
