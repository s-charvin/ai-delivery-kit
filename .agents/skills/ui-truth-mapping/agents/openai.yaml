interface:
  display_name: "UI Truth Mapping"
  short_description: "Map Figma to a real host-stack component plus preview"
  default_prompt: "Use $ui-truth-mapping to freeze a real project component per unit (Flutter: widget + flutter test --update-goldens). Show the user the preview PNG absolute path. Do not generate ui-contract.html. Do not translate HTML into Flutter."
