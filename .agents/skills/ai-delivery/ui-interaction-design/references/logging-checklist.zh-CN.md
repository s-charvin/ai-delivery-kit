# 日志检查清单

当单独的管理支持界面可用时，按此顺序使用。

1. 确定目标项目和子需求范围
2. 为当前操作追加开始日志
3. 在变更工件之前读取当前阻塞和状态
4. 在 `.ai-delivery/` 内执行工件写入
5. 通过有治理的支持界面记录任何状态转换、阻塞创建或阻塞恢复
6. 追加完成或失败日志，包含足够详细的信息以便中断后恢复

如果管理支持界面尚不可用：

- 将工件真相保留在 `.ai-delivery/` 中
- 在 `decisions.md` 或本地执行记录中记载缺失的治理依赖项
- 不要在批准的架构之外发明第二个状态或日志存储
