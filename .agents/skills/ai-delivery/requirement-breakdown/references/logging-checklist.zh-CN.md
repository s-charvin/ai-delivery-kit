# 日志记录检查清单

当独立的管理支持面可用时，请按此顺序使用。

1. 确定目标项目和子需求范围
2. 为当前操作附加一条开始日志
3. 在修改工件之前读取当前阻塞项和状态
4. 在 `.ai-delivery/` 内执行工件写入
5. 通过受治理的管理支持面记录任何状态转换、阻塞项创建或阻塞项恢复
6. 附加一条完成或失败日志，附带足够详细的信息以便中断后恢复

如果管理支持面尚不可用：

- 将工件真相保留在 `.ai-delivery/` 中
- 在 `decisions.md` 或本地执行说明中记录缺失的受治理依赖
- 不要在已批准的架构之外发明第二个状态或日志存储
