# 日志检查清单

在单独的管理支持界面可用时，按此顺序使用。

1. 确定目标项目和子需求范围
2. 为当前操作追加开始日志
3. 在变更工件前读取当前阻止器和状态
4. 在 `.ai-delivery/` 内执行工件写入
5. 通过受治理的支持界面记录任何状态转换、阻止器创建或阻止器恢复
6. 追加完成或失败日志，提供足够细节以便中断后恢复

如果管理支持界面尚不可用：

- 将工件真相保留在 `.ai-delivery/` 中
- 在 `decisions.md` 或本地执行笔记中记录缺失的受治理依赖
- 不要在批准的架构之外臆造第二个状态或日志存储
