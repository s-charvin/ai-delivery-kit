<!-- ai-delivery-template-language
Before using this template, translate every human-readable heading, label, and prose passage into the user's current conversation language. Keep IDs, status values, paths, commands, timestamps, hashes, and placeholders unchanged. Remove this comment from the instantiated template.
-->

# Delivery Report - <req-id>

> Automatically generated by the `archive` action after all sub-requirements are archived. This file is the delivery summary for `runtime_mode=completed`; canonical artifacts remain in their requirement directories.

## Overview

- Requirement ID: <req-id>
- Archived at: <archived_at>
- Sub-requirement count: <subreq_count>
- Canonical artifacts: each sub-requirement's artifacts remain in its original directory; this report is only a summary.

## Sub-requirements

| Sub-requirement | Status | Canonical artifacts | Verification |
|-----------------|--------|-------------------|--------------|
<subreq_rows>

## Verification Summary

- Each sub-requirement's `verification.md` is signed and includes the three required language-neutral markers for review rounds, verification commands/results, and sign-off.
- `validate-artifact-layout.py --verify-archive` may additionally verify legacy snapshots when an older repository contains them; current in-place archives do not require snapshots.

## Changes and Follow-up

- Future requirement changes must use a new `<req-id>/` directory. The completed requirement directory remains the historical source of truth.
- Do not create a second copy of canonical requirement artifacts during archive.
