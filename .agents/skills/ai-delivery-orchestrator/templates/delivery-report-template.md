<!-- ai-delivery-template-language
Before using this template, translate every human-readable heading, label, and prose passage into the user's current conversation language. Keep IDs, status values, paths, commands, timestamps, hashes, and placeholders unchanged. Remove this comment from the instantiated template.
-->

# Delivery Report - <req-id>

> Automatically generated by the `archive` action after all sub-requirements are frozen and archived. This file is the immutable delivery summary for `runtime_mode=completed`, where every sub-requirement is `archived`.

## Overview

- Requirement ID: <req-id>
- Archived at: <archived_at>
- Sub-requirement count: <subreq_count>
- Frozen snapshots: each sub-requirement's `archive/<ISO-ts>/` plus `MANIFEST.json` (SHA-256 verified)

## Sub-requirements

| Sub-requirement | Status | Archived snapshot | Verification |
|-----------------|--------|-------------------|--------------|
<subreq_rows>

## Verification Summary

- Each sub-requirement's `verification.md` is signed and includes the three required language-neutral markers for review rounds, verification commands/results, and sign-off.
- `validate-artifact-layout.py --verify-archive` passed SHA-256 verification for every archived snapshot with no tampering detected.

## Changes and Follow-up

- Future requirement changes must use a new `<req-id>/` directory. This directory is immutable and remains available as a read-only reference.
- Machine verification of archive immutability is based on the SHA-256 manifest at each `archive/<ISO-ts>/MANIFEST.json`.
