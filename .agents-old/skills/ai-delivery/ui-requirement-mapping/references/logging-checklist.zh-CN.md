# 日志检查清单

当独立的管理支持界面可用时，请按此顺序操作。

1. 确定目标项目和子需求范围
2. 为当前操作追加开始日志
3. 在变更产物之前读取当前阻塞项和状态
4. 在 `.ai-delivery/` 内执行产物写入
5. 通过受治理管理界面记录任何状态转换、阻塞项创建或阻塞项恢复
6. 追加完成或失败日志，附带足够细节以便中断后恢复

如果管理支持界面尚不可用：

- 将产物真值保留在 `.ai-delivery/` 中
- 在 `decisions.md` 或本地执行备注中记录缺失的受治理依赖
- 不得在已批准的架构之外创建第二个状态或日志存储
