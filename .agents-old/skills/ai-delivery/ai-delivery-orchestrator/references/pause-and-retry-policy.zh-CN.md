# 暂停与重试策略

- 自动重试：普通第二阶段 2 次、审查修复 2 次、视觉验收修复 2 次、临时工具故障 1 次。
- 仅在 `CP-001 tasks_ready_user_confirmation` 或 `CP-002 hard_blocker_pause` 处暂停。
- `CP-001`：仅在所有可执行切片达到 `tasks_ready` 后暂停。
- `CP-002`：在硬阻塞器、不可恢复的门控失败或缺失人工决策时暂停。
- `review` 首次失败和视觉验收首次失败必须在升级之前进入自动修复循环。
